_prefs: 
  disabled: []
  inspector: false
  mapid: ''
  rev: ''
  saveCenter: true
attribution: ''
center: 
  - 6.1263
  - 49.8056
  - 10
description: ''
Layer: 
  - id: landuse
    Datasource: 
      dbname: gis
      extent: -20037508.34,-20037508.34,20037508.34,20037508.34
      geometry_field: ''
      geometry_table: ''
      host: ''
      key_field: ''
      max_size: 512
      password: ''
      port: ''
      table: |-
        (
        SELECT landuse AS class, z_order, way
          FROM planet_osm_polygon
          WHERE
            (landuse = 'cemetery' OR amenity = 'hospital')
            AND z(!scale_denominator!) >= 10
            AND way && !bbox!
        UNION ALL
        SELECT 'park' AS class, z_order, way
          FROM planet_osm_polygon
          WHERE
            (
              (
                landuse = 'village_green'
                OR leisure IN ('park', 'playground')
              )
              AND z(!scale_denominator!) >= 10
            ) OR (
              (
                leisure IN ('national_reserve', 'nature_reserve', 'golf_course')
                OR boundary = 'national_park'
              )
              -- TODO:
              --landuse IN ('conservation')
              AND z(!scale_denominator!) >= 7
            )
            AND way && !bbox!
        UNION ALL
        SELECT 'school' AS class, z_order, way
          FROM planet_osm_polygon
          WHERE
            amenity IN ('school', 'university')
            AND z(!scale_denominator!) >= 10
            AND way && !bbox!
        UNION ALL
        SELECT 'wood' AS class, z_order, way
          FROM planet_osm_polygon
          WHERE
            landuse IN ('wood', 'forest')
            AND z(!scale_denominator!) >= 7
            AND way && !bbox!
        ) landuse
      type: postgis
      user: ''
    description: ''
    fields: 
      class: String
      z_order: Number
    properties: 
      "buffer-size": 8
    srs: +proj=merc +a=6378137 +b=6378137 +lat_ts=0.0 +lon_0=0.0 +x_0=0.0 +y_0=0.0 +k=1.0 +units=m +nadgrids=@null +wktext +no_defs +over
  - id: waterway
    Datasource: 
      dbname: gis
      extent: -20037508.34,-20037508.34,20037508.34,20037508.34
      geometry_field: ''
      geometry_table: ''
      host: ''
      key_field: ''
      max_size: 512
      password: ''
      port: ''
      table: |-
        (
        SELECT way, waterway AS class
          FROM planet_osm_line
          WHERE waterway IN ('river', 'canal')
          AND z(!scale_denominator!) >= 8
          AND way && !bbox!
        UNION ALL
        SELECT way, waterway AS class
          FROM planet_osm_line
          WHERE waterway IN ('stream', 'stream_intermittent')
          AND z(!scale_denominator!) >= 13
          AND way && !bbox!
        UNION ALL
        SELECT way, waterway AS class
          FROM planet_osm_line
          WHERE waterway IN ('drain', 'ditch')
          AND z(!scale_denominator!) >= 15
          AND way && !bbox!
        ) waterway
      type: postgis
      user: ''
    description: ''
    fields: 
      class: String
    properties: 
      "buffer-size": 8
    srs: +proj=merc +a=6378137 +b=6378137 +lat_ts=0.0 +lon_0=0.0 +x_0=0.0 +y_0=0.0 +k=1.0 +units=m +nadgrids=@null +wktext +no_defs +over
  - id: water
    Datasource: 
      dbname: gis
      extent: -20037508.34,-20037508.34,20037508.34,20037508.34
      geometry_field: ''
      geometry_table: ''
      host: ''
      key_field: ''
      max_size: 512
      password: ''
      port: ''
      table: |-
        (
        SELECT way
          FROM planet_osm_polygon
          WHERE
            (
              "natural" = 'water'
              OR landuse = 'reservoir'
              OR landuse = 'pond'
            )
            AND way && !bbox!
        ) water
      type: postgis
      user: ''
    description: ''
    fields: {}
    properties: 
      "buffer-size": 8
    srs: +proj=merc +a=6378137 +b=6378137 +lat_ts=0.0 +lon_0=0.0 +x_0=0.0 +y_0=0.0 +k=1.0 +units=m +nadgrids=@null +wktext +no_defs +over
  - id: tunnel
    Datasource: 
      dbname: gis
      extent: -20037508.34,-20037508.34,20037508.34,20037508.34
      geometry_field: ''
      geometry_table: ''
      host: ''
      key_field: ''
      max_size: 512
      password: ''
      port: ''
      table: |-
        (
        SELECT way, 'motorway' AS class
          FROM  planet_osm_line
          WHERE
            tunnel = 'yes'
            AND highway IN ('motorway')
            AND z(!scale_denominator!) >= 5
            AND way && !bbox!
        UNION ALL
        SELECT way, 'motorway_link' AS class
          FROM  planet_osm_line
          WHERE
            tunnel = 'yes'
            AND highway IN ('motorway_link')
            AND z(!scale_denominator!) >= 13
            AND way && !bbox!
        UNION ALL
        SELECT way, 'main' AS class
          FROM  planet_osm_line
          WHERE
            tunnel = 'yes'
            AND highway IN ('primary', 'primary_link', 'trunk', 'trunk_link')
            AND z(!scale_denominator!) >= 5
            AND way && !bbox!
        UNION ALL
        SELECT way, 'main' AS class
          FROM  planet_osm_line
          WHERE
            tunnel = 'yes'
            AND highway IN ('secondary', 'secondary_link')
            AND z(!scale_denominator!) >= 9
            AND way && !bbox!
        UNION ALL
        SELECT way, 'main' AS class
          FROM  planet_osm_line
          WHERE
            tunnel = 'yes'
            AND highway IN ('tertiary', 'tertiary_link')
            AND z(!scale_denominator!) >= 12
            AND way && !bbox!
        UNION ALL
        SELECT way, 'street' AS class
          FROM  planet_osm_line
          WHERE
            tunnel = 'yes'
            AND highway IN ('residential', 'unclassified', 'living_street')
            AND z(!scale_denominator!) >= 12
            AND way && !bbox!
        UNION ALL
        SELECT way, 'street_limited' AS class
          FROM  planet_osm_line
          WHERE
            tunnel = 'yes'
            AND (highway IN ('pedestrian', 'construction') or access = 'private')
            AND z(!scale_denominator!) >= 12
            AND way && !bbox!
        UNION ALL
        SELECT way, 'service' AS class
          FROM  planet_osm_line
          WHERE
            tunnel = 'yes'
            AND highway IN ('service', 'track')
            AND z(!scale_denominator!) >= 15
            AND way && !bbox!
        UNION ALL
        SELECT way, 'driveway' AS class
          FROM  planet_osm_line
          WHERE
            tunnel = 'yes'
            AND highway IN ('driveway')
            AND z(!scale_denominator!) >= 15
            AND way && !bbox!
        UNION ALL
        SELECT way, 'path' AS class
          FROM  planet_osm_line
          WHERE
            tunnel = 'yes'
            AND highway IN ('path', 'cycleway', 'ski', 'steps', 'bridleway')
            AND z(!scale_denominator!) >= 15
            AND way && !bbox!
        UNION ALL
        SELECT way, 'major_rail' AS class
          FROM  planet_osm_line
          WHERE
            tunnel = 'yes'
            AND highway IN ('rail', 'monorail', 'narrow_gauge', 'subway', 'tram')
            AND z(!scale_denominator!) >= 12
            AND way && !bbox!
        UNION ALL
        SELECT way, 'minor_rail' AS class
          FROM  planet_osm_line
          WHERE
            tunnel = 'yes'
            AND highway IN ('funicular', 'light_rail', 'preserved')
            AND z(!scale_denominator!) >= 15
            AND way && !bbox!
        ) tunnel
      type: postgis
      user: ''
    description: ''
    fields: 
      class: String
    properties: 
      "buffer-size": 8
    srs: +proj=merc +a=6378137 +b=6378137 +lat_ts=0.0 +lon_0=0.0 +x_0=0.0 +y_0=0.0 +k=1.0 +units=m +nadgrids=@null +wktext +no_defs +over
  - id: road
    Datasource: 
      dbname: gis
      extent: -20037508.34,-20037508.34,20037508.34,20037508.34
      geometry_field: way
      geometry_table: ''
      host: ''
      key_field: ''
      max_size: 512
      password: ''
      port: ''
      table: |
        (
        SELECT way, 'motorway' AS class
          FROM  planet_osm_line
          WHERE
            bridge IS NULL
            AND tunnel IS NULL
            AND highway IN ('motorway')
            AND z(!scale_denominator!) >= 5
            AND way && !bbox!
        UNION ALL
        SELECT way, 'motorway_link' AS class
          FROM  planet_osm_line
          WHERE
            bridge IS NULL
            AND tunnel IS NULL
            AND highway IN ('motorway_link')
            AND z(!scale_denominator!) >= 13
            AND way && !bbox!
        UNION ALL
        SELECT way, 'main' AS class
          FROM  planet_osm_line
          WHERE
            bridge IS NULL
            AND tunnel IS NULL
            AND highway IN ('primary', 'primary_link', 'trunk', 'trunk_link')
            AND z(!scale_denominator!) >= 5
            AND way && !bbox!
        UNION ALL
        SELECT way, 'main' AS class
          FROM  planet_osm_line
          WHERE
            bridge IS NULL
            AND tunnel IS NULL
            AND highway IN ('secondary', 'secondary_link')
            AND z(!scale_denominator!) >= 9
            AND way && !bbox!
        UNION ALL
        SELECT way, 'main' AS class
          FROM  planet_osm_line
          WHERE
            bridge IS NULL
            AND tunnel IS NULL
            AND highway IN ('tertiary', 'tertiary_link')
            AND z(!scale_denominator!) >= 12
            AND way && !bbox!
        UNION ALL
        SELECT way, 'street' AS class
          FROM  planet_osm_line
          WHERE
            bridge IS NULL
            AND tunnel IS NULL
            AND highway IN ('residential', 'unclassified', 'living_street')
            AND z(!scale_denominator!) >= 12
            AND way && !bbox!
        UNION ALL
        SELECT way, 'street' AS class
          FROM  planet_osm_line
          WHERE
            bridge IS NULL
            AND tunnel IS NULL
            AND highway IN ('residential', 'unclassified', 'living_street')
            AND z(!scale_denominator!) >= 12
            AND way && !bbox!
        UNION ALL
        SELECT way, 'street_limited' AS class
          FROM  planet_osm_line
          WHERE
            bridge IS NULL
            AND tunnel IS NULL
            AND (highway IN ('pedestrian', 'construction') or access = 'private')
            AND z(!scale_denominator!) >= 12
            AND way && !bbox!
        UNION ALL
        SELECT way, 'service' AS class
          FROM  planet_osm_line
          WHERE
            bridge IS NULL
            AND tunnel IS NULL
            AND highway IN ('service', 'track')
            AND z(!scale_denominator!) >= 15
            AND way && !bbox!
        UNION ALL
        SELECT way, 'driveway' AS class
          FROM  planet_osm_line
          WHERE
            bridge IS NULL
            AND tunnel IS NULL
            AND highway IN ('driveway')
            AND z(!scale_denominator!) >= 15
            AND way && !bbox!
        UNION ALL
        SELECT way, 'path' AS class
          FROM  planet_osm_line
          WHERE
            bridge IS NULL
            AND tunnel IS NULL
            AND highway IN ('path', 'cycleway', 'ski', 'steps', 'bridleway')
            AND z(!scale_denominator!) >= 15
            AND way && !bbox!
        UNION ALL
        SELECT way, 'major_rail' AS class
          FROM  planet_osm_line
          WHERE
            bridge IS NULL
            AND tunnel IS NULL
            AND railway IN ('rail', 'monorail', 'narrow_gauge', 'subway', 'tram')
            AND z(!scale_denominator!) >= 12
            AND way && !bbox!
        UNION ALL
        SELECT way, 'minor_rail' AS class
          FROM  planet_osm_line
          WHERE
            bridge IS NULL
            AND tunnel IS NULL
            AND railway IN ('funicular', 'light_rail', 'preserved')
            AND z(!scale_denominator!) >= 15
            AND way && !bbox!
        ) road
      type: postgis
      user: ''
    description: ''
    fields: 
      class: String
    properties: 
      "buffer-size": 8
    srs: +proj=merc +a=6378137 +b=6378137 +lat_ts=0.0 +lon_0=0.0 +x_0=0.0 +y_0=0.0 +k=1.0 +units=m +nadgrids=@null +wktext +no_defs +over
  - id: bridge
    Datasource: 
      dbname: gis
      extent: -20037508.34,-20037508.34,20037508.34,20037508.34
      geometry_field: ''
      geometry_table: ''
      host: ''
      key_field: ''
      max_size: 512
      password: ''
      port: ''
      table: |-
        (
        SELECT way, 'motorway' AS class
          FROM  planet_osm_line
          WHERE
            bridge = 'yes'
            AND highway IN ('motorway')
            AND z(!scale_denominator!) >= 5
            AND way && !bbox!
        UNION ALL
        SELECT way, 'motorway_link' AS class
          FROM  planet_osm_line
          WHERE
            bridge = 'yes'
            AND highway IN ('motorway_link')
            AND z(!scale_denominator!) >= 13
            AND way && !bbox!
        UNION ALL
        SELECT way, 'main' AS class
          FROM  planet_osm_line
          WHERE
            bridge = 'yes'
            AND highway IN ('primary', 'primary_link', 'trunk', 'trunk_link')
            AND z(!scale_denominator!) >= 5
            AND way && !bbox!
        UNION ALL
        SELECT way, 'main' AS class
          FROM  planet_osm_line
          WHERE
            bridge = 'yes'
            AND highway IN ('secondary', 'secondary_link')
            AND z(!scale_denominator!) >= 9
            AND way && !bbox!
        UNION ALL
        SELECT way, 'main' AS class
          FROM  planet_osm_line
          WHERE
            bridge = 'yes'
            AND highway IN ('tertiary', 'tertiary_link')
            AND z(!scale_denominator!) >= 12
            AND way && !bbox!
        UNION ALL
        SELECT way, 'street' AS class
          FROM  planet_osm_line
          WHERE
            bridge = 'yes'
            AND highway IN ('residential', 'unclassified', 'living_street')
            AND z(!scale_denominator!) >= 12
            AND way && !bbox!
        UNION ALL
        SELECT way, 'street_limited' AS class
          FROM  planet_osm_line
          WHERE
            bridge = 'yes'
            AND (highway IN ('pedestrian', 'construction') or access = 'private')
            AND z(!scale_denominator!) >= 12
            AND way && !bbox!
        UNION ALL
        SELECT way, 'service' AS class
          FROM  planet_osm_line
          WHERE
            bridge = 'yes'
            AND highway IN ('service', 'track')
            AND z(!scale_denominator!) >= 15
            AND way && !bbox!
        UNION ALL
        SELECT way, 'driveway' AS class
          FROM  planet_osm_line
          WHERE
            bridge = 'yes'
            AND highway IN ('driveway')
            AND z(!scale_denominator!) >= 15
            AND way && !bbox!
        UNION ALL
        SELECT way, 'path' AS class
          FROM  planet_osm_line
          WHERE
            bridge = 'yes'
            AND highway IN ('path', 'cycleway', 'ski', 'steps', 'bridleway')
            AND z(!scale_denominator!) >= 15
            AND way && !bbox!
        UNION ALL
        SELECT way, 'major_rail' AS class
          FROM  planet_osm_line
          WHERE
            bridge = 'yes'
            AND highway IN ('rail', 'monorail', 'narrow_gauge', 'subway', 'tram')
            AND z(!scale_denominator!) >= 12
            AND way && !bbox!
        UNION ALL
        SELECT way, 'minor_rail' AS class
          FROM  planet_osm_line
          WHERE
            bridge = 'yes'
            AND highway IN ('funicular', 'light_rail', 'preserved')
            AND z(!scale_denominator!) >= 15
            AND way && !bbox!
        ) bridge
      type: postgis
      user: ''
    description: ''
    fields: 
      class: String
    properties: 
      "buffer-size": 8
    srs: +proj=merc +a=6378137 +b=6378137 +lat_ts=0.0 +lon_0=0.0 +x_0=0.0 +y_0=0.0 +k=1.0 +units=m +nadgrids=@null +wktext +no_defs +over
  - id: admin
    Datasource: 
      dbname: gis
      extent: -20037508.34,-20037508.34,20037508.34,20037508.34
      geometry_field: ''
      geometry_table: ''
      host: ''
      key_field: ''
      max_size: 512
      password: ''
      port: ''
      table: |-
        (
        SELECT admin_level, way, 0 AS maritime
          FROM planet_osm_polygon
          WHERE
            boundary = 'administrative'
            AND (
              admin_level = '2'
              OR ( admin_level = '4' AND z(!scale_denominator!) >= 5 )
            )
            AND way && !bbox!
        ) admin
      type: postgis
      user: ''
    description: ''
    fields: 
      admin_level: String
      maritime: Number
    properties: 
      "buffer-size": 8
    srs: +proj=merc +a=6378137 +b=6378137 +lat_ts=0.0 +lon_0=0.0 +x_0=0.0 +y_0=0.0 +k=1.0 +units=m +nadgrids=@null +wktext +no_defs +over
  - id: country_label
    Datasource: 
      dbname: gis
      extent: -20037508.34,-20037508.34,20037508.34,20037508.34
      geometry_field: ''
      geometry_table: ''
      host: ''
      key_field: ''
      max_size: 512
      password: ''
      port: ''
      table: |-
        (
        SELECT way, name, CASE
              WHEN CAST(population AS int) >= 250000000 THEN 1
              WHEN CAST(population AS int) >= 100000000 AND CAST(population AS int) < 250000000 THEN 2
              WHEN CAST(population AS int) >= 50000000 AND CAST(population AS int) < 100000000 THEN 3
              WHEN CAST(population AS int) >= 25000000 AND CAST(population AS int) < 50000000 THEN 4
              WHEN CAST(population AS int) >= 10000000 AND CAST(population AS int) < 25000000 THEN 5
              WHEN CAST(population AS int) < 10000000 THEN 6
            END scalerank
          FROM planet_osm_point
          WHERE
            place = 'country'
            AND way && !bbox!
        ) country_label
      type: postgis
      user: ''
    description: ''
    fields: 
      name: String
      scalerank: Number
    properties: 
      "buffer-size": 8
    srs: +proj=merc +a=6378137 +b=6378137 +lat_ts=0.0 +lon_0=0.0 +x_0=0.0 +y_0=0.0 +k=1.0 +units=m +nadgrids=@null +wktext +no_defs +over
  - id: place_label
    Datasource: 
      dbname: gis
      extent: -20037508.34,-20037508.34,20037508.34,20037508.34
      geometry_field: ''
      geometry_table: ''
      host: ''
      key_field: ''
      max_size: 512
      password: ''
      port: ''
      table: |-
        (
        SELECT DISTINCT ON(labelgrid(way, 64, 1)) * FROM(
        /* TODO calculate real ranks and ldir */
        SELECT way, osm_id, name, place AS "type",
            'SE' AS ldir, 1 AS localrank
          FROM planet_osm_point
          WHERE
            place = 'city'
            AND way && !bbox!
        UNION ALL
        SELECT way, osm_id, name, place AS "type",
            'SE' AS ldir, 1 AS localrank
          FROM planet_osm_point
          WHERE
            place = 'town'
            AND z(!scale_denominator!) >= 7
            AND way && !bbox!
        UNION ALL
        SELECT way, osm_id, name, place AS "type",
            'SE' AS ldir, 1 AS localrank
          FROM planet_osm_point
          WHERE
            place = 'village'
            AND z(!scale_denominator!) >= 11
            AND way && !bbox!
        UNION ALL
        SELECT way, osm_id, name, place AS "type",
            'SE' AS ldir, 1 AS localrank
          FROM planet_osm_point
          WHERE
            place IN ('hamlet', 'suburb','neighbourhood')
            AND z(!scale_denominator!) >= 13
            AND way && !bbox!
        ) data
        ) place_label
      type: postgis
      user: ''
    description: ''
    fields: 
      ldir: String
      localrank: Number
      name: String
      osm_id: Number
      type: String
    properties: 
      "buffer-size": 64
    srs: +proj=merc +a=6378137 +b=6378137 +lat_ts=0.0 +lon_0=0.0 +x_0=0.0 +y_0=0.0 +k=1.0 +units=m +nadgrids=@null +wktext +no_defs +over
  - id: road_label
    Datasource: 
      dbname: gis
      extent: -20037508.34,-20037508.34,20037508.34,20037508.34
      geometry_field: ''
      geometry_table: ''
      host: ''
      key_field: ''
      max_size: 512
      password: ''
      port: ''
      table: |-
        (
        SELECT way, name, ref, char_length(ref) AS reflen,
          ROUND(merc_length(way)) AS len, 'motorway' AS class
          FROM planet_osm_roads
          WHERE
            bridge IS NULL AND tunnel IS NULL AND highway IN ('motorway')
            AND z(!scale_denominator!) >= 5
            AND linelabel(z(!scale_denominator!), name, way)
            AND way && !bbox!
        UNION ALL
        SELECT way, name, ref, char_length(ref) AS reflen,
          ROUND(merc_length(way)) AS len, 'motorway_link' AS class
          FROM planet_osm_roads
          WHERE
            bridge IS NULL AND tunnel IS NULL AND highway IN ('motorway_link')
            AND z(!scale_denominator!) >= 13
            AND linelabel(z(!scale_denominator!), name, way)
            AND way && !bbox!
        UNION ALL
        SELECT way, name, ref, char_length(ref) AS reflen,
          ROUND(merc_length(way)) AS len, 'main' AS class
          FROM planet_osm_roads
          WHERE
            bridge IS NULL AND tunnel IS NULL AND highway IN ('primary',
          'primary_link', 'trunk', 'trunk_link')
            AND z(!scale_denominator!) >= 5
            AND linelabel(z(!scale_denominator!), name, way)
            AND way && !bbox!
        UNION ALL
        SELECT way, name, ref, char_length(ref) AS reflen,
          ROUND(merc_length(way)) AS len, 'main' AS class
          FROM planet_osm_roads
          WHERE
            bridge IS NULL AND tunnel IS NULL AND highway IN ('secondary',
          'secondary_link')
            AND z(!scale_denominator!) >= 9
            AND linelabel(z(!scale_denominator!), name, way)
            AND way && !bbox!
        UNION ALL
        SELECT way, name, ref, char_length(ref) AS reflen,
          ROUND(merc_length(way)) AS len, 'main' AS class
          FROM planet_osm_roads
          WHERE
            bridge IS NULL AND tunnel IS NULL AND highway IN ('tertiary',
          'tertiary_link')
            AND z(!scale_denominator!) >= 12
            AND linelabel(z(!scale_denominator!), name, way)
            AND way && !bbox!
        UNION ALL
        SELECT way, name, ref, char_length(ref) AS reflen,
          ROUND(merc_length(way)) AS len, 'street' AS class
          FROM planet_osm_roads
          WHERE
            bridge IS NULL AND tunnel IS NULL AND highway IN ('residential',
          'unclassified', 'living_street')
            AND z(!scale_denominator!) >= 12
            AND linelabel(z(!scale_denominator!), name, way)
            AND way && !bbox!
        UNION ALL
        SELECT way, name, ref, char_length(ref) AS reflen,
          ROUND(merc_length(way)) AS len, 'street_limited' AS class
          FROM planet_osm_roads
          WHERE
            bridge IS NULL AND tunnel IS NULL AND (highway IN ('pedestrian',
          'construction') OR access = 'private')
            AND z(!scale_denominator!) >= 12
            AND linelabel(z(!scale_denominator!), name, way)
            AND way && !bbox!
        UNION ALL
        SELECT way, name, ref, char_length(ref) AS reflen,
          ROUND(merc_length(way)) AS len, 'service' AS class
          FROM planet_osm_roads
          WHERE
            bridge IS NULL AND tunnel IS NULL AND highway IN ('service',
          'track')
            AND z(!scale_denominator!) >= 15
            AND linelabel(z(!scale_denominator!), name, way)
            AND way && !bbox!
        UNION ALL
        SELECT way, name, ref, char_length(ref) AS reflen,
          ROUND(merc_length(way)) AS len, 'driveway' AS class
          FROM planet_osm_roads
          WHERE
            bridge IS NULL AND tunnel IS NULL AND highway IN ('driveway')
            AND z(!scale_denominator!) >= 15
            AND linelabel(z(!scale_denominator!), name, way)
            AND way && !bbox!
        UNION ALL
        SELECT way, name, ref, char_length(ref) AS reflen,
          ROUND(merc_length(way)) AS len, 'path' AS class
          FROM planet_osm_roads
          WHERE
            bridge IS NULL AND tunnel IS NULL AND highway IN ('path',
          'cycleway', 'ski', 'steps', 'bridleway')
            AND z(!scale_denominator!) >= 15
            AND linelabel(z(!scale_denominator!), name, way)
            AND way && !bbox!
        ) AS road_label
      type: postgis
      user: ''
    description: ''
    fields: 
      class: String
      len: Number
      name: String
      ref: String
      reflen: Number
    properties: 
      "buffer-size": 8
    srs: +proj=merc +a=6378137 +b=6378137 +lat_ts=0.0 +lon_0=0.0 +x_0=0.0 +y_0=0.0 +k=1.0 +units=m +nadgrids=@null +wktext +no_defs +over
maxzoom: 13
minzoom: 4
name: osm2pgsql-osm-bright
